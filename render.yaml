services:
  - type: web
    name: the-75-project-api
    env: docker
    rootDir: backend
    plan: free
    envVars:
      - key: NIM_API_KEY
        sync: false
      - key: OPENROUTER_API_KEY
        sync: false
